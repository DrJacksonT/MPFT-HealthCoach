import Link from "next/link";
import { requireServerPermission } from "@/src/auth/server";
import { releaseRows } from "@/src/research/staff-data";

export default async function StaffReleasesPage() {
  await requireServerPermission("configuration:manage"); const rows = await releaseRows();
  return <main id="main-content" className="app-content"><nav className="breadcrumbs"><Link href="/staff">Staff workspace</Link><span>/</span><span>Release gates</span></nav><div className="app-title"><div><p className="eyebrow">Fail closed</p><h1>Release gates</h1><p>Environment flags and named, dated database approvals must both pass.</p></div><span className="tag tag--warning">Live pilot closed</span></div><div className="release-list">{rows.map((release) => { const manifest = release.manifest; const missing = [manifest.governanceApproval !== true && "governance", manifest.clinicalSafetyApproval !== true && "clinical safety", manifest.deploymentApproval !== true && "deployment"].filter(Boolean); return <article key={release.id}><div><span className={`tag ${release.status === "authorised" && missing.length === 0 ? "tag--open" : "tag--warning"}`}>{release.status}</span><h2>{release.study}</h2><p>{release.type.replaceAll("_", " ")} · {release.environment} · {release.version}</p></div><div><strong>{missing.length ? `Closed: ${missing.join(", ")} approval missing` : "Database record authorised"}</strong><p className="microcopy">Named/dated authorisation: {release.authorisedAt?.toLocaleString("en-GB") ?? "missing"}</p></div></article>; })}</div><div className="notice"><strong>Technical completion cannot authorise a live pilot.</strong> Approval records must be created through a separately governed change-control procedure; this UI intentionally has no self-authorise button.</div></main>;
}
